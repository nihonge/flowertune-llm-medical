# 第三章 隐私保护与确权协作框架设计

本章将详细阐述本文提出的大语言模型隐私保护与确权协作框架。针对传统联邦微调中存在的内存溢出、中心化信任瓶颈以及模型资产越权滥用等痛点，本章首先明确系统的总体设计目标与安全威胁假设；随后对架构中的核心实体进行严格定义；最后从全局视角，深度剖析系统从任务发布、密态聚合到模型按劳确权分发的全生命周期协作流与通信协议。

### 3.1 总体设计目标与安全威胁假设

#### 3.1.1 总体设计目标
在大规模医疗语料等敏感数据场景下，构建联邦大模型面临着算力、隐私与资产权属的三重博弈。为打破“数据孤岛”并建立健康的数据协作生态，本框架的设计需满足以下三大核心目标：
1. 计算与通信的极度轻量化：针对边缘节点（如各级医院）算力受限的现状，系统需避免对百亿参数大模型执行全量同态加密。通过引入低秩自适应（LoRA）与选择性加密机制，将内存消耗与通信负载控制在消费级 GPU 可承受的范围内。
2. 聚合过程的无条件隐私安全：在梯度或权重上传阶段，必须从密码学底层（而非启发式混淆）杜绝中心服务器通过反向工程推断出底层患者病历特征，确保数据“可用不可见”。
3. 模型资产的细粒度按劳分配：全局大模型作为多方算力与数据凝结的高价值资产，其分发权不能被单一中心节点垄断。系统需实现基于节点历史贡献度的事前访问控制，彻底根除联邦生态中的“搭便车（Free-riding）”行为。

#### 3.1.2 安全威胁模型假设
在本文构建的联邦拓扑网络中，假定不存在完全可信的第三方硬件（如 TEE）。各实体的安全威胁假设如下：
* 聚合服务器（Server）：假定为“诚实且好奇（Honest-but-Curious）”。即服务器会诚实地执行联邦平均算法并下发参数，但其有强烈动机窥探各参与方上传的权重以反推隐私数据；同时，在缺乏制约时，Server 极易将高价值的全局模型据为己有或越权售卖。
* 边缘参与节点（Clients）：假定部分节点为“自私且被动恶意（Selfish and Passive Malicious）”。此类节点可能试图通过少量数据参与训练以套取全局模型，或与其他节点共谋以骗取解密权限，但不会主动发起投毒攻击破坏全局模型收敛。

### 3.2 系统总体架构与核心实体定义

为了在上述严苛的安全假设下实现大模型微调协作，本框架采用了物理拓扑与逻辑控制流分离的分层架构设计。如图 3-1 所示，本系统包含三大核心物理实体。

（此处预留空间：插入 图 3-1 大模型隐私保护与确权协作总体架构图，建议占半页纸）

1. 边缘计算节点（Clients / 数据拥有方）
边缘计算节点是底层敏感数据（如医疗问答文本）的实际控制者。在本框架中，Clients 具备一定的本地微调算力，其核心职责包括：
* 本地高效微调：利用本地隐私数据对预训练模型（如 Mistral-7B）执行 LoRA 微调，计算出局部权重增量 $\Delta W$。
* 选择性密态封装：调用本地 TenSEAL 引擎，使用公钥 $pk$ 对 LoRA 模块中包含核心语义的高维投影矩阵（如 $q\_proj, v\_proj$）执行 CKKS 同态加密，并在本地留存未加密的非敏感层参数。
* 贡献度申明与模型拉取：在满足系统设定的历史贡献度阈值时，向智能合约申请属性基解密私钥 $SK$，从而合法获取最终的全局大模型。

2. 聚合服务器与任务发布者（Server / Task Publisher）
Server 拥有极高的网络带宽与内存资源。其主要职责为：
* 联邦调度与同步：利用分布式引擎协调大规模并发节点的通信，收集各 Client 上传的密文参数。
* 纯密态盲算聚合：在不解密的前提下，直接对所有 Clients 提交的密态多项式执行逐元素同态加法（Element-wise FedAvg）。
* 资产对称锁定与 ABE 封装：在密态聚合完成后，Server 在内存中瞬间完成全局模型的明文转换，并立即使用 AES 算法对高价值模型进行强锁定。随后，调用去中心化多授权属性加密（DMA-CP-ABE）对 AES 密钥进行封装。

3. 去中心化信任中枢（Smart Contract & IPFS）
为解决单点故障与模型资产越权分发问题，本框架创新性地引入了区块链与分布式存储双层网络：
* IPFS 分布式存储池：负责承载大体积的大模型权重文件。Server 将加密后的全局模型上传至 IPFS 网络，获取唯一的、防篡改的内容寻址哈希（CID）。
* 区块链智能合约（Smart Contract）：作为“赛博裁判”，负责永久存储 IPFS 返回的 CID，并持久化记录各边缘节点的历史贡献度。当节点发起下载请求时，合约自动校验其属性是否满足策略树 $\mathcal{T}$。

### 3.3 全生命周期协作工作流与时序设计

基于上述实体定义，本框架单次大模型联邦微调生命周期包含六大核心步骤，构成了一个闭环的信任协作协议。其具体的时序交互过程如图 3-2 所示。

（此处预留空间：插入 图 3-2 联邦协作与确权全生命周期时序图 Sequence Diagram，建议占大半页纸）

步骤一：系统初始化与任务广播
任务发布者（Server）在区块链智能合约上注册联邦大模型微调任务，广播预训练模型的初始化权重链接，并公开 CKKS 的同态加密公钥 $pk$ 以及 ABE 系统的公共参数。

步骤二：端侧选择性微调（Local Fine-Tuning）
各参与节点下载初始模型，在本地安全域内执行微调，更新 LoRA 旁路矩阵参数 $A$ 和 $B$。

步骤三：端侧分块密态封装（Chunking & Encryption）
微调完成后，Clients 对更新后的高维 LoRA 权重执行选择性加密。针对超出 CKKS 单密文容量上限（如多项式阶数 $N/2$）的注意力层张量，系统底层自动触发分块（Chunking）机制，将其切分为多个密文多项式序列，随后将密态张量 $ct$ 打包上传至 Server。

步骤四：中心化同态安全聚合（Secure Aggregation）
Server 接收到满足并发阈值的节点密文后，在全密态环境下对各个密文车厢执行同态加法：
$$ct_{global} = \sum_{i=1}^{K} ct_{i} \pmod q$$
此过程彻底阻断了梯度反演攻击路径。

步骤五：模型极速确权与 IPFS 存证（Asset Locking & Proofing）
Server 利用同态私钥 $sk$ 对聚合后的全局密文进行解密。在解密生成的瞬间，Server 生成包含节点贡献度要求的访问策略树 $\mathcal{T}$（如“参与轮数 $\geq 5$ AND 医疗机构等级 = 三甲”），锁定模型本体并上传 IPFS，最后将哈希 CID 写入智能合约。

步骤六：基于贡献度的按劳分配（Attribute-Based Distribution）
完成微调的 Client 根据自身在链上记录的贡献度属性，向多授权机构申请 ABE 私钥分片。若该节点的属性集合 $S$ 满足策略树 $\mathcal{T}$（即 $S \models \mathcal{T}$），即可成功重构 AES 密钥，并从 IPFS 通过 CID 拉取并解包获得全局大模型。

### 3.4 核心数据通信结构设计

在大规模并发的联邦网络中，为保证密态张量的高效传输与异构设备的兼容性，本框架对客户端与服务器之间的通信载荷（Payload）进行了严格的结构化设计。

（此处预留空间：插入 图 3-3 密态参数通信载荷结构示意图，可以画成多个相连的字节块）

端云之间交互的核心数据包结构序列化为二进制流，其内部包含三大核心字段：
1. 元数据头部（Header Metadata）：包含客户端 ID、当前训练轮次（Round）、聚合数据量权重（Data Size）以及操作校验码，用于 Server 端的请求鉴权与聚合权重分配。
2. 明文参数区（Plaintext Parameters）：存放无需加密的非核心层参数，如 `lm_head` 层权重，以 FP16 精度直接传输，极大降低了通信冗余。
3. 密态多项式序列区（Ciphertext Polynomials）：存放经过 CKKS 加密的 $q\_proj$ 等注意力层核心参数。由于张量被分块，该区域包含多个连续的密文对象序列 $(c_0^{(j)}, c_1^{(j)})$，并在尾部附加了对应的缩放因子（Scale）以供 Server 执行近似加法。

### 3.5 本章小结
本章从全局系统工程的视角，详细设计了面向大语言模型的隐私保护与确权协作框架。通过明确系统的设计目标与安全假设，定义了边缘节点、聚合服务器与区块链三大核心实体。在此基础上，系统性地推演了包含“本地微调、分块加密、同态聚合、属性封装、链上存证、按劳拉取”的六步闭环工作流，并给出了底层通信载荷的结构规范。本章的架构设计为第四章核心安全算法的数学推导与代码级实现奠定了坚实的系统级框架基础。